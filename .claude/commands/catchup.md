# Catchup Command

Read all changed files in the current git branch to understand what has been modified.

Steps:
1. Run `git diff --name-only main...HEAD` to find all changed files
2. Use the Read tool to read each changed file
3. Provide a concise summary of the changes found
